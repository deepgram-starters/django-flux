[meta]
title = "Django Flux"
description = "Get started using Deepgram's Flux real-time transcription with this Django demo app"
author = "Deepgram DX Team <devrel@deepgram.com> (https://developers.deepgram.com)"
repository = "https://github.com/deepgram-starters/django-flux"
useCase = "flux"
language = "python"
framework = "django"
tags = ["flux", "streaming", "speech-to-text", "real-time", "turn-detection", "django", "python"]

[check]
command = ["git --version", "python3 --version"]
message = "All prerequisites installed"

[install.pre]
command = "git submodule update --init --recursive"
message = "Submodules initialized"

[install]
command = ["pip install -r requirements.txt", "cd frontend && corepack pnpm install"]
message = "Dependencies installed"

[install.post]
command = "test -f .env || cp sample.env .env"
message = "Environment configuration ready"

[start.pre]
command = "test -f .env || (echo '❌ .env not found. Run: cp sample.env .env' && exit 1)"
message = "Environment validated"

[start]
command = ["python3 app.py", "cd frontend && pnpm run dev -- --port 8080 --no-open"]
parallel = true
message = "Application running on http://localhost:8080"

[update]
command = "git submodule update --remote --merge"
message = "Submodules updated"

[clean]
command = ["rm -rf __pycache__", "rm -rf frontend/node_modules", "rm -rf frontend/.vite", "rm -rf starter/__pycache__"]
message = "Build artifacts cleaned"

[test]
command = "cd contracts && ./tests/run-flux-app.sh"
message = "Tests complete"

[eject-frontend]
command = "make eject-frontend"
message = "Frontend ejected successfully"
